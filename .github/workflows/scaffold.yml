name: Nuclear scaffold checks

on:
  pull_request:
  push:
    branches:
      - experimental/nuclear-scaffold-v0.1

jobs:
  proof:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Python compile
        run: python -m compileall -q haunted_blender
      - name: Contract tests
        run: python -m unittest discover -s tests -v
      - name: CLI smoke
        run: python -m haunted_blender --help
